package test;

import org.testng.annotations.Test;
import main.java.com.sirma.itt.javacourse.findcenter.*;

/**
 * 
 * @author Nikolay Ch
 * 
 */
public class FindCenterTest {
	/**
	 * Tests if the method is finding the center of weight of array with random
	 * values.
	 */
	@Test
	public void TestFindCenterOfArray() {
		Main.main(null);
	}
}
